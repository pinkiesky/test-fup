# Test

## First run

```bash
$ npm install && npm run build && npm run db:init
```

## Run app.js

```bash
$ npm run app:start
```

## Run incremental sync

```bash
$ npm run sync:incr
```

## Run full sync

```bash
$ npm run sync:full
```